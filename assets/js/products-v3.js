(() => { const panel = document.querySelector('.category-panel'); const close = document.querySelector('.panel-close'); const open = () => { panel.classList.add('is-open'); panel.setAttribute('aria-hidden', 'false'); close.focus(); }; const hide = () => { panel.classList.remove('is-open'); panel.setAttribute('aria-hidden', 'true'); }; document.querySelectorAll('.category-trigger').forEach(button => button.addEventListener('click', open)); close.addEventListener('click', hide); document.addEventListener('keydown', event => { if (event.key === 'Escape') hide(); }); })();
