const menuButton=document.querySelector('.menu-button'),nav=document.querySelector('.site-nav');if(menuButton&&nav){menuButton.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',open);menuButton.setAttribute('aria-label',open?'Close menu':'Open menu')});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuButton.setAttribute('aria-expanded','false')}))}document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());document.querySelectorAll('[data-filter]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-filter]').forEach(b=>b.classList.remove('active'));button.classList.add('active');const filter=button.dataset.filter;document.querySelectorAll('[data-project]').forEach(card=>card.hidden=filter!=='all'&&card.dataset.project!==filter)}));document.querySelectorAll('[data-enquiry]').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(form),body=[...f.entries()].map(([k,v])=>k+': '+v).join('\n');location.href='mailto:info@zamtechsdnbhd.com?subject='+encodeURIComponent('ZAMTECH website enquiry')+'&body='+encodeURIComponent(body)}));const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;if(!reduce&&'IntersectionObserver'in window){document.querySelectorAll('section,.card,.sector').forEach(x=>x.classList.add('reveal'));new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');e.target.style.transitionDelay=Math.min([...e.target.parentElement.children].indexOf(e.target)*35,160)+'ms'}}),{threshold:.1}).observe(document.body);document.querySelectorAll('.reveal').forEach(x=>new IntersectionObserver(e=>e[0].isIntersecting&&x.classList.add('visible'),{threshold:.08}).observe(x))}
